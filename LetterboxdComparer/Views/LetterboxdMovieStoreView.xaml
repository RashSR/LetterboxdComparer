<UserControl x:Class="LetterboxdComparer.Views.LetterboxdMovieStoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LetterboxdComparer.Presenter">

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Movies}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    Width="*" />

                <DataGridTextColumn Header="Year"
                                    Binding="{Binding ReleaseYear}"
                                    Width="80" />

                <DataGridTextColumn Header="UUID"
                                    Binding="{Binding Uuid}"
                                    Width="150" />

                <DataGridTextColumn Header="URL"
                                    Binding="{Binding LetterboxdUrl}"
                                    Width="200" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
