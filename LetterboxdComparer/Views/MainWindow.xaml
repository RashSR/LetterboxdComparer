<Window x:Class="LetterboxdComparer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LetterboxdComparer"
        Title="Letterboxd Comparer"
        Width="900"
        Height="500">

    <Window.DataContext>
        <local:MainWindowPresenter/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT MENU -->
        <StackPanel Background="#2D2D30">
            <Button Content="Statistics"
                    Command="{Binding ShowStatisticsCommand}"
                    Margin="10"/>
            <Button Content="Details"
                    Command="{Binding ShowDetailsCommand}"
                    Margin="10"/>
        </StackPanel>

        <!-- CONTENT -->
        <ContentControl Grid.Column="1">
            <ContentControl.Style>
                <Style TargetType="ContentControl">

                    <!-- default view -->
                    <Setter Property="Content">
                        <Setter.Value>
                            <local:StatisticsView/>
                        </Setter.Value>
                    </Setter>

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding CurrentView}"
                                     Value="{x:Static local:AppView.Statistics}">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <local:StatisticsView/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding CurrentView}"
                                     Value="{x:Static local:AppView.Details}">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <local:DetailsView/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>

                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</Window>
